# Copy this file to .env, then restart `npm run dev` (or rebuild with
# `npm run build`) for changes to take effect.

# Where the simple backend (in /server) is running.
VITE_API_URL=http://localhost:4000

# Only needed if the backend has API_KEY set (see server/.env.example).
VITE_API_KEY=
